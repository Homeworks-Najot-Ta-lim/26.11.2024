export function FirstButton({ children }) {
    return (

        <>
        <button className="transparendBtn">{children}</button>
        </>
    )
  }
  
  export function SecondButton({ children }) {
    return (

        <>
        <button className="blackBtn">{children}</button>
        </>
    )
  }